class test_maxcomdiv{

float maxcomdiv (float a, float b){
        float dividendo,divisor,resto;
	if(a > b){
		dividendo = a;
		divisor = b;
	}
	else{
		dividendo = b;
		divisor = a;
	}
	resto = 1.0;
	while ((resto != 0.0)){
		resto = dividendo % divisor;
		dividendo = divisor;
		divisor = resto;
	}
	return dividendo;
}

void main(){
    externinvk("printf", float , "mcd%d", maxcomdiv(6.0,9.0 ));
    return ;
}

}
